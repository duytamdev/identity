server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/learning_springboot3
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
jwt:
  singerKey: "dxpgce6byerdem2t1jltk26i4pj7v4wr"
  valid-duration: 15 # 5 second
  refreshable-duration: 120 # 120 second